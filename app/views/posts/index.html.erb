<div class="all-posts">
<% @posts.each do |post| %>
  <span><%= post.id%></span><a href="<%= post.url %>"><%= post.title %></a><br>
  <span><%= link_to "post", post_path(post) %></span>
<% end %>
</div>
